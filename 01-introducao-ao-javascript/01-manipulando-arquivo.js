var y = 0;
var x = 1;

var z = 2;

/* ax + b = 0  */

// Função Afim -> f(1) = a.1 + b

/*
 * Variáveis: Eles podem variar ao longo do processo ou do seu código. (v)
 * Constantes: São valores estabelecidos que não podem ser alterados em seguida. (C)
 */

// var preco = 2;
var desconto = 0.2;
const PRECO = 2;
var total = PRECO - desconto;